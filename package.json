{
  "name": "cypress_test",
  "version": "1.0.0",
  "description": "Pakwheels Web desktop smoke test autmatoion testing suite.",
  "main": "index.js",
  "scripts": {
    "triggerAllTests-headless": "npx cypress run",
    "triggerAllTests-headed": "npx cypress run --headed",
    "triggerAllTests-chrome": "npx cypress run --headed --browser edge",
    "triggerAllTests-webdriveruni": "npx cypress run --spec cypress/e2e/*",
    "junit-merge": "npx junit-merge -d cypress/results/junit -o cypress/results/junit/results.xml",
    "delete-junit-report": "rm -rf cypress/results/junit/results.xml",
    "delete-results": "rm -rf cypress/results/* || true",
    "mochawesome-merge": "npx mochawesome-merge cypress/results/mochawesome/*.json > mochawesome.json && npx marge mochawesome.json",
    "delete-mochawesome-report": "rm -rf mochawesome-report/* || true",
    "cypress-regression-pack": "npm run delete-results && npm run delete-mochawesome-report && npm run triggerAllTests-headed && npm run mochawesome-merge"
  },
  "author": "umer",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.10.0",
    "cypress-file-upload": "^5.0.8",
    "cypress-plugin-retries": "^1.5.2",
    "mocha": "^6.2.3"
  },
  "dependencies": {
    "cypress-multi-reporters": "^1.6.1",
    "mocha-junit-reporter": "^2.0.2",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1",
    "mochawesome-report-generator": "^6.2.0"
  }
}
